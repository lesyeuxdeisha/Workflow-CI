name: HeartDiseaseProject

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 50}
      max_depth: {type: int, default: 5}
    command: "python modelling_tuning.py"